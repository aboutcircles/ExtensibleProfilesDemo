/**
 * Vitest global setup
 * (currently empty – add polyfills / env vars / vi.mock() calls here)
 */

// Example: make sure the Node crypto polyfill is active
import '../src/utils/crypto-polyfill';